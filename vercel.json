{
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "framework": null,
  "rewrites": [
    { "source": "/build/(.*)", "destination": "/build/$1" },
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
